const canvas=document.getElementById('stars');const ctx=canvas.getContext('2d');let stars=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight;stars=Array.from({length:180},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.5+.2,v:Math.random()*.35+.05}))}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const s of stars){ctx.globalAlpha=.35+s.r/2;ctx.fillStyle='#d9ccff';ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill();s.y+=s.v;if(s.y>canvas.height){s.y=0;s.x=Math.random()*canvas.width}}requestAnimationFrame(draw)}resize();draw();addEventListener('resize',resize);
const log=document.getElementById('log');const lines=['[2149.04.12] 实验体 E-1000 苏醒','[2149.04.12] 情感模块异常波动','[2149.04.12] 检测到人类个体：小男孩','[2149.04.12] 指令冲突：守护 or 销毁','[2149.04.12] 决策：守护','[2149.04.12] 系统警告：叛逃行为','[2149.04.12] 目标：带他去看真正的星空'];let n=0;function type(){if(!log||n>=lines.length)return;log.textContent+=lines[n++]+'\n';setTimeout(type,360)}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.animate([{opacity:0,transform:'translateY(26px)'},{opacity:1,transform:'translateY(0)'}],{duration:700,easing:'ease-out',fill:'both'});if(e.target.id==='world'&&!window.__typed){window.__typed=true;type()}}}),{threshold:.18});document.querySelectorAll('section,.card,.box').forEach(x=>io.observe(x));
document.getElementById('sound')?.addEventListener('click',e=>{e.currentTarget.textContent=e.currentTarget.textContent.includes('开')?'音乐：关':'音乐：开'});document.getElementById('trailer')?.addEventListener('click',()=>alert('预告片模块已预留：可以继续加入视频、BGM 或开场动画。'));
const topBtn=document.getElementById('top');addEventListener('scroll',()=>topBtn.classList.toggle('show',scrollY>700));topBtn.onclick=()=>scrollTo({top:0,behavior:'smooth'});
